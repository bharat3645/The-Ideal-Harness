{
  "name": "@ideal-harness/orchestrate",
  "version": "0.1.0",
  "type": "module",
  "description": "The Ideal Harness orchestration & control flow: durable task ledger, tool registry, loop/no-progress guard, spend governor, API retry/backoff, session resume/checkpoint, plus the subagent-driven-development and brainstorming skills.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "ideal-harness-orchestrate": "./dist/cli/index.js"
  },
  "files": [
    "dist",
    "skills",
    ".claude-plugin"
  ],
  "dependencies": {
    "@ideal-harness/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/test/**/*.test.js\"",
    "clean": "rm -rf dist dist-test"
  }
}
