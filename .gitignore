node_modules/
dist/
dist-test/
*.tsbuildinfo
.turbo/
coverage/
.DS_Store
*.log
.env
.env.*
!.env.example

# Per-project memory store (project-local by design; never committed).
.ideal-harness/

# User-local Claude Code settings (statusline wiring; scripts/setup.mjs regenerates it).
.claude/settings.local.json

# Claude Code's own runtime lock for scheduled tasks (per-machine, per-session PID/timestamp
# state) -- was accidentally committed in the initial commit; never project data.
.claude/scheduled_tasks.lock
